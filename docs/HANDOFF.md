# MoodBook — HANDOFF: повний стан проєкту (7 вересня 2026)

Читати першим у новій сесії. Тут: стан на зараз, усі затверджені власником рішення, повний список того, що **ще не зроблено**, карта файлів, доступи (без секретів — репозиторій публічний) і як перевіряти.

Деталі в окремих файлах: `CLAUDE.md` (правила роботи), `docs/PRICING.md` (ціновий аналіз), `docs/OPS.md` (квоти, сховище, промокоди, моніторинг), `docs/ARCHITECTURE-APPS.md` (сайт+апки+оплата), `docs/TEAM-REVIEW-2026-09-05.md` (аудит), `docs/DESIGN.md` (візуальний контракт).

> ⚠️ **Репозиторій github.com/portianoi1993/moodbook- ПУБЛІЧНИЙ.** Ніколи не записувати сюди ключі, токени й паролі. Усі секрети живуть лише в змінних середовища Vercel.

---

## 1. Стан на зараз

- **Live: https://moodbook.ink** — працює, HTTPS, домен підключений 6.09.2026. `www.moodbook.ink` і стара адреса `moodbook-six.vercel.app` роблять 308-перенаправлення на основний домен.
- Прод = гілка `main`, автодеплой Vercel. Останній коміт: `73441c4`.
- **Що вміє продукт:** вводиш книгу (підказки розуміють назву, автора або обидва в будь-якому порядку) → AI складає 6 довгих YouTube-міксів + 5 сцен із самої книги + 5 музичних стилів, які комбінуються між собою → грає у вбудованому плеєрі (розгорнути / згорнути / міні-режим / закрити). Полиця книг, лайки, історія прослуханого. Reading Card — картинка 1080×1350 для шерингу. 10 мов інтерфейсу. Денна і нічна тема. Блокування РФ.
- **Тарифи (працюють у інтерфейсі, оплата ще не підключена):** Free = 3 повні книги назавжди; Pro = $59.99/рік (за замовчуванням) або $9.99/міс; Founding Reader = $79 довічно, перші 100. Для української локалі ₴1 490 / ₴249 / ₴1 990.
- **Промокоди працюють на проді:** унікальні, одноразові, згорають на сервері.
- Стек: vanilla JS + HTML + CSS, serverless-функції на Vercel, сховище Upstash Redis (безкоштовне). Без фреймворків, без збірки. Дані користувача — у його браузері (localStorage).

---

## 2. Що власник затвердив (не переглядати без нового рішення)

| # | Рішення | Статус |
|---|---|---|
| 1 | **Ціни:** Pro $59.99/рік (головний, за замовчуванням), $9.99/міс, Founding Reader $79 довічно для перших 100. Гривневі ціни ₴1 490 / ₴249 / ₴1 990 | ✅ в інтерфейсі |
| 2 | **Free = 3 повні книги назавжди** (не на день), з усіма сценами і стилями; книги з полиці граються безкоштовно назавжди | ✅ зроблено |
| 3 | **Промокоди:** унікальні, одноразові, окремий код під кожного блогера, дають Pro на рік | ✅ зроблено |
| 4 | **Російської мови немає і не буде**, територія РФ заблокована | ✅ зроблено |
| 5 | **10 мов:** англійська, українська, іспанська, французька, німецька, італійська, португальська, польська, китайська, японська | ✅ зроблено |
| 6 | **Нічна тема** («Lamp Light»), автоматично за системою + перемикач | ✅ зроблено |
| 7 | **Дизайн:** тепла крем-пастельна бібліотека, шрифти Fraunces + Figtree, книжкові елементи. Лого і шрифтову пару не міняти | ✅ зафіксовано в DESIGN.md |
| 8 | **Апки на паузі:** Apple відкладено надовго, Google Play першим, коли повернемось | ⏸ відкладено |
| 9 | **SEO і маркетинг — після підключення платіжки** | ⏸ відкладено |
| 10 | **Gemini:** поки безкоштовний тариф, платний увімкне пізніше | ⏸ відкладено |
| 11 | **Домен moodbook.ink** підключити (потрібно для перевірки WayForPay) | ✅ зроблено |
| 12 | **Пошти на сайті немає** (прибрано за рішенням власника) | ✅ зроблено |
| 13 | Reading Card як вірусний механізм | ✅ зроблено |

---

## 3. ЩО ЩЕ НЕ ЗРОБЛЕНО

### 3.1 Оплата (головний блокер усього іншого)

| Що | Стан | Хто робить |
|---|---|---|
| Вибір провайдера: WayForPay / Stripe / PayPal | **Власник подав документи у WayForPay, чекає затвердження.** Домен уже перевірять, бо працює | власник |
| КВЕД у ФОП: 63.99 (інформаційні послуги), 47.91 (роздрібна торгівля через інтернет), опційно 62.01 | **не зроблено**, WayForPay вимагає | власник, через «Дію» |
| Опис діяльності в договорі WayForPay | текст підготовлено в чаті 7.09 | власник |
| Кнопка оплати на сайті замість «Payments open soon» | не зроблено, чекає на реквізити | я, після затвердження |
| Вебхук платіжки → права доступу | не зроблено | я |
| Founding Reader: лічильник 100 місць і закриття продажу | не зроблено (зараз лише картка з обіцянкою) | я |
| Сторінка засновників з іменами | не зроблено | я |
| Stripe і PayPal | Stripe потребує закордонної компанії, PayPal-бізнес в Україні обмежений. Рішення відкладено | власник |

**Важливо:** до появи оплати не можна обіцяти синхронізацію між пристроями. Зараз її немає в описі Pro, і так має лишатись до Supabase.

### 3.2 Акаунти і синхронізація (передумова чесного Pro)

- **Supabase не інтегровано.** Потрібні: вхід за посиланням на email + Google, таблиці `profiles`, `entitlements`, `shelf`, `liked_tracks`, `promo_codes`, ендпоінт `/api/me`, міграція даних із браузера в акаунт.
- Без цього: почистив браузер — втратив полицю і Pro; оплата теж не може бути прив'язана до людини.
- План описано в `docs/ARCHITECTURE-APPS.md`.

### 3.3 Просування сайту (усе відкладено до платіжки, крім позначеного)

| Що | Деталі | Стан |
|---|---|---|
| **Сторінки книг для Google** | `/book/<назва>` для топ-100–200 книг зі статичним текстом і кнопкою «грати». Головний органічний канал: люди шукають «music for reading Dune» | не зроблено |
| Один `<h1>` на сторінку | зараз три (по одному на кожну вкладку) — Google бачить усі | не зроблено |
| Розмітка FAQPage | секція питань уже готова за змістом | не зроблено |
| Заголовок сайту з ключовими словами | зараз «Every book has a sound», гарний, але без пошукових слів | не зроблено |
| **og:image 1200×630** | зараз квадратна іконка; посилання в месенджерах виглядають бідно | чекає на створення Telegram/Instagram |
| **Лист очікування або Telegram** | зараз людина, якій сподобалось, іде і забуває. Це найбільша діра до появи оплати | не зроблено |
| Підказка «зроби Reading Card» після першого треку + UTM-мітка в посиланні картки | картка є, але схована за маленькою кнопкою | не зроблено |
| Лічильник підборів як соціальний доказ | «12 400 саундтреків складено» | не зроблено |
| Reddit (r/books, r/Fantasy, r/printSF, r/suggestmeabook), BookTok/Reels, Product Hunt | пости про книги з картками, не реклама сервісу | не зроблено |
| Vercel Web Analytics | скрипт у коді вже є, треба увімкнути в панелі | **не увімкнено** |

### 3.4 Технічна стійкість

| Що | Деталі | Стан |
|---|---|---|
| **Квота YouTube** | зараз ~100 пошуків на день. Форма «YouTube Data API Services Audit and Quota Extension» заповнена на 7.09.2026 (заявник — фізособа, проєкт 469116481466, просимо 100 000 одиниць/день + 1 000 search.list/день). **Лишилось:** адреса, 4 скріншоти-докази, атестації, Submit — робить власник | **заповнено, не подано** |
| **Квота Google Books** | 1 000 запитів на день. **Заявку подати неможливо:** у консолі поле не приймає значення > 1 000, а форма `support.google.com/books/contact/api_quota_request` видалена Google (редірект на довідку). Пом'якшено кодом: при вичерпанні підказки йдуть з Open Library без квоти | **закрито, дій не потрібно** |
| Upstash Redis | ✅ підключено 7.09, спільний кеш + промокоди + чесний ліміт запитів | зроблено |
| Моніторинг UptimeRobot | безкоштовний, на `/api/health?probe=1` | не зроблено |
| Старий Google-ключ у публічній історії git | `AIzaSyD_GX…` (лежав у проєкті `ninth-bonito-468417-b2`, акаунт halk93, під назвою «API key 1») — **видалений 7.09.2026**. Прод не постраждав: він працює на ключі «MoodBook server» | ✅ зроблено |
| Зайві змінні `OPENAI_API_KEY`, `SC_CLIENT_ID` у Vercel | не використовуються, видалити | не зроблено |
| `AI_MODEL` оновити на `gemini-3.6-flash` | зараз стара назва, код мігрує сам, але робить зайвий запит | не зроблено |
| Node.js 20 → 24 у Vercel | зафіксовано через `"engines": {"node": "24.x"}` у `package.json` (коміт `69357aa`), Vercel читає це сам | ✅ зроблено |
| Smoke-тест прод-API за розкладом | 5 запитів, тривога на помилки | не зроблено |

### 3.5 Продуктові дрібниці

1. Демо-картка в героі виглядає як живий плеєр, але статична: зробити клікабельною або прибрати напис «Now playing».
2. Порожня полиця може одразу пропонувати 3 популярні книги.
3. Перевірити контраст тексту на шести пастельних кольорах.
4. На мобільному після першого відтворення вмикати міні-плеєр за замовчуванням.
5. Переклади 8 мов зроблені без носіїв — попросити перших користувачів вичитати.
6. `www.moodbook.ink` у Vercel стоїть як Production (перенаправлення робить код, не панель).

### 3.6 Апки (пауза)

Capacitor-оболонка навколо цього ж сайту, Google Play першим, RevenueCat для покупок, спільні права доступу з сайтом. Головний ризик: YouTube не грає у фоні в мобільній апці. Повний план у `docs/ARCHITECTURE-APPS.md`.

---

## 4. Доступи: де що лежить

> Секрети не в репозиторії. Паролі й токени вводить лише власник; у чаті 7.09.2026 є значення `ADMIN_TOKEN`.

| Сервіс | Адреса | Акаунт | Для чого | Що там налаштовано |
|---|---|---|---|---|
| **Домен** | ap.www.namecheap.com | Namecheap, вхід власника | moodbook.ink до 11.06.2027, автопродовження | DNS = Namecheap BasicDNS; записи `A @ 216.198.79.1` і `CNAME www → …vercel-dns-017.com`. WHOIS підтверджено 6.09.2026 (до того домен був призупинений) |
| **Хостинг** | vercel.com/mykhailo-portianoi-s-projects/moodbook | Vercel, план Hobby | сайт і serverless-функції | домени, змінні середовища, сховище, деплої |
| **Код** | github.com/portianoi1993/moodbook- | GitHub | гілка `main` = прод | **публічний репозиторій** |
| **AI** | aistudio.google.com | Google (в AI Studio показувався halk93@gmail.com) | Gemini, безкоштовний тариф | ключ у Vercel як `AI_API_KEY` |
| **YouTube + книги** | console.cloud.google.com, проєкт «My First Project» | Google Cloud | пошук музики, обкладинки | ключ «MoodBook server» обмежений двома API, у Vercel як `YT_API_KEY` |
| **Сховище** | Vercel → Storage → upstash-kv-coral-forest | Upstash через Vercel Marketplace, план Free | кеш, промокоди, ліміт запитів | регіон iad1, автовидалення вимкнене, 500 тис. команд/міс |
| **Пошта** | portianoi1993@gmail.com | Gmail | листи Namecheap, Vercel | у Chrome це другий акаунт (u/1), перший — halk93@gmail.com |
| **Платіжка** | wayforpay.com | ФОП власника | оплата | документи подано, чекає затвердження |

### Змінні середовища у Vercel (Production + Preview)

| Змінна | Що це | Стан |
|---|---|---|
| `AI_API_KEY` | ключ Gemini | є |
| `AI_BASE_URL` | `https://generativelanguage.googleapis.com/v1beta/openai` | є |
| `AI_MODEL` | `gemini-2.5-flash` (варто оновити на `gemini-3.6-flash`) | є |
| `YT_API_KEY` | YouTube + Google Books | є |
| `KV_REST_API_URL`, `KV_REST_API_TOKEN`, `KV_URL`, `REDIS_URL`, `KV_REST_API_READ_ONLY_TOKEN` | сховище Upstash, додані автоматично | є |
| `ADMIN_TOKEN` | пароль для створення промокодів | є (значення у чаті 7.09) |
| `OPENAI_API_KEY`, `SC_CLIENT_ID` | старі, не використовуються | видалити |

**Після зміни будь-якої змінної потрібен передеплой**, інакше функції її не побачать.

---

## 5. Промокоди: як користуватись

Створити код для блогера (рік Pro, одна активація):
```
https://moodbook.ink/api/promo?admin=ТОКЕН&create=1&note=Ім'я блогера&days=365
```
У відповіді буде код виду `MB-XXXX-XXXX` і готове посилання `https://moodbook.ink/?code=MB-XXXX-XXXX`.

- Довічний код: `&days=36500`. Багаторазовий (наприклад, на 50 людей): `&uses=50`.
- Список і хто активував: `&list=1`. Відкликати: `&revoke=MB-XXXX-XXXX`.
- Активація: поле «Promo code» у paywall чи в акаунті, або відкрити посилання. Регістр і дефіси неважливі. Друга спроба тим самим кодом отримує відмову.
- Видані станом на 7.09.2026: власнику довічний (використано), другу-тестувальнику на рік (не використано), один тестовий (використано).

---

## 6. Карта файлів

```
index.html            Лендинг + застосунок (Discover / Library / Account), плеєр, paywall, модалка Reading Card.
                      Англійські тексти = ключі перекладів.
privacy.html, terms.html, blocked.html   Політика, умови, заглушка для РФ.
css/app.css           Стилі: денна і нічна теми, плеєр, тарифи, модалка, меню мов.
js/app.js             Логіка: пошук, результати, плеєр, полиця, лайки, історія, paywall, промокоди, тема, мови.
js/fx.js              Анімації: полотно в героі, появи, магнітні кнопки, стрічка.
js/i18n.js            Мовний рушій: вибір мови, підвантаження словника, переклад сторінки.
js/lang/*.js          9 словників (uk, es, fr, de, it, pt, pl, zh, ja), по 229 ключів.
js/card.js            Reading Card на canvas.
api/analyze.js        Один виклик AI → книга, опис, 5 сцен, 5 стилів, 6 треків. Кеш 7 днів.
api/books.js          Підказки: Google Books + Open Library, пошук за назвою і автором.
api/search.js         Пошук міксу на YouTube, кеш 7 днів, запасні мікси при вичерпаній квоті.
api/promo.js          Промокоди: створення, список, відкликання, одноразова активація.
api/cover.js          Проксі обкладинок для картки.
api/health.js         Діагностика: `?probe=1` перевіряє AI і сховище, `?models=1` показує моделі.
lib/ai.js             Ланцюг AI-провайдерів, обхід вичерпаної квоти через сусідні моделі.
lib/store.js          Сховище Upstash + кеш у пам'яті; локально працює без сховища.
lib/http.js           Обмеження запитів, блокування РФ, CORS, кеш-заголовки.
lib/fallback.js       Жанровий плейлист, коли весь AI недоступний.
lib/evergreen.js      Перевірені довгі відео на випадок вичерпаної квоти YouTube.
vercel.json           Перенаправлення на moodbook.ink, блокування РФ, заголовки, ліміт 60 с на функцію.
scripts/dev.mjs       Локальний сервер :3939. `npm run dev`.
scripts/mock-ai.mjs   Фейковий AI на :3940 для тестів без ключа.
scripts/check-imports.mjs    Перед пушем: ловить синтаксичні помилки у функціях.
scripts/check-i18n.mjs       Після зміни текстів: однаковий набір ключів у всіх мовах.
scripts/check-untranslated.mjs  Шукає англійські рядки без перекладу.
scripts/i18n-apply-patch.mjs + i18n-patch-*.json   Пакетне оновлення словників.
docs/                 HANDOFF.md (цей файл), PRICING.md, OPS.md, ARCHITECTURE-APPS.md,
                      TEAM-REVIEW-2026-09-05.md, DESIGN.md, SKILLS-AUDIT.md, AUDIT-2026-09-05-v2.md.
```

---

## 7. Як працювати і перевіряти

- Локально: `npm run dev` → http://localhost:3939. AI локально — мок: `node scripts/mock-ai.mjs`, а сервер запускати зі змінними `AI_API_KEY=mock AI_BASE_URL=http://localhost:3940/v1`. Для промокодів локально ще `ADMIN_TOKEN=test123`. Файлу `.env.local` немає, тому YouTube і Google Books локально не працюють — реальні перевірки на проді.
- Перед пушем: `node scripts/check-imports.mjs`. Після зміни текстів: `node scripts/check-i18n.mjs` і `node scripts/check-untranslated.mjs`.
- Після зміни CSS чи JS підняти версію `?v=YYYYMMDDxN` **одночасно** в `index.html`, `privacy.html`, `terms.html` **і в import-ах усередині `js/app.js`**. Поточна: `20260907a1`.
- Перевірка прода: `https://moodbook.ink/api/health?probe=1` — має бути `"store": {"ok": true}` і робочий провайдер AI.
- Дев-сервер перечитує `api/`, але не `lib/` — після змін у `lib/` перезапускати.
- Код зі зворотними слешами правити через Edit/Write, не через bash-heredoc.

## 8. Відомі граблі

- **Безкоштовна квота Gemini рахується окремо на кожну модель.** При вичерпанні код сам переходить на сусідні моделі, у крайньому разі віддає жанровий плейлист із кнопкою «Спробувати ще раз».
- **YouTube 403** → до опівночі за тихоокеанським часом граються перевірені постійні мікси, користувач бачить одну підказку.
- **localStorage прив'язаний до домену:** після переїзду на moodbook.ink полиця й Pro зі старої адреси не переносяться.
- **Браузер кешує `/js` на годину:** без підняття версії в import-ах користувачі отримують старий модуль і білий екран.
- Vercel-прев'ю захищені входом, відкриваються лише в браузері власника.
